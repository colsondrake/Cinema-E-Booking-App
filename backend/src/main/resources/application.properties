# MongoDB connection
spring.data.mongodb.uri=mongodb+srv://jtc94964:3AtpODLLrUeDCncC@dev-cinema.oyipgwi.mongodb.net/?retryWrites=true&w=majority&appName=dev-cinema
spring.data.mongodb.database=cesdb

# Server configuration
server.port=8080

# Optional: logging level
logging.level.org.springframework.data.mongodb=DEBUG

# Mail settings (read from environment when available). Defaults target Gmail SMTP.
# Use environment variables instead of committing credentials. Recommended names shown.
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.protocol=${SPRING_MAIL_PROTOCOL:smtp}

# Username/password should be provided via environment variables for security.
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}

# SMTP properties
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_STARTTLS:true}
spring.mail.properties.mail.smtp.starttls.required=${SPRING_MAIL_STARTTLS_REQUIRED:false}
spring.mail.properties.mail.debug=${SPRING_MAIL_DEBUG:false}

# Connection timeouts (ms)
spring.mail.properties.mail.smtp.connectiontimeout=${SPRING_MAIL_CONN_TIMEOUT:5000}
spring.mail.properties.mail.smtp.timeout=${SPRING_MAIL_TIMEOUT:5000}
spring.mail.properties.mail.smtp.writetimeout=${SPRING_MAIL_WRITE_TIMEOUT:5000}

# Base URL used when building links in emails (override for dev/prod)
app.base.url=${APP_BASE_URL:http://localhost:8080}

